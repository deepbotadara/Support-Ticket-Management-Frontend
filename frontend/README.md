# Frontend

Next.js frontend for the Support Ticket Management System.

## Run

```bash
npm install
npm run dev
```

Frontend runs on `http://localhost:3001` when backend is already using `3000`.

## Backend Dependency

Frontend expects backend API at `http://localhost:3000` through rewrite configuration.

Run backend before login or ticket operations.
